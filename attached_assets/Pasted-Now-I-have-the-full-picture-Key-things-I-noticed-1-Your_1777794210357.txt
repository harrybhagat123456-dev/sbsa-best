Now I have the full picture. Key things I noticed:

1. Your bot uses **Pyrogram** — NOT python-telegram-bot. My previous prompts were wrong library!
2. You already have `topic_handler.py` — the new feature goes into a **new separate file** `auto_topic_creator.py`
3. `main.py` needs only **2 surgical lines added** — one import, one register call

Here is the precise prompt:

---

> ## Task: Add Auto Topic Creator Feature to Existing Pyrogram Bot
>
> **DO NOT touch or rewrite `main.py` logic. Make only the two small additions described below.**
>
> ---
>
> ### STEP 1 — Create a new file: `auto_topic_creator.py`
>
> This file must follow the same pattern as all other handler files in this project (e.g. `topic_handler.py`) — it exports one function `register_auto_topic_handlers(bot)`.
>
> **Full logic of this file:**
>
> ```python
> import asyncio
> from pyrogram import Client, filters
> from pyrogram.types import Message
> from logs import logging
>
> # In-memory state store: {user_id: {"state": "WAIT_GROUP_ID", "topics": [...]}}
> _user_state = {}
>
> def parse_topics_from_text(text: str) -> list[str]:
>     """
>     Parses lines like: 📌 Arithmetic — 591 links
>     Extracts only the name between 📌 and — (em dash)
>     Returns a list of topic name strings, each capped at 128 chars.
>     """
>     topics = []
>     for line in text.splitlines():
>         if "📌" in line and "—" in line:
>             try:
>                 name = line.split("📌")[1].split("—")[0].strip()
>                 if name:
>                     topics.append(name[:128])
>             except Exception:
>                 continue
>     return topics
>
> def register_auto_topic_handlers(bot: Client):
>
>     @bot.on_message(filters.private & filters.document)
>     async def handle_txt_file(client: Client, m: Message):
>         # Only handle .txt files
>         if not m.document or not m.document.file_name.endswith(".txt"):
>             return
>
>         status = await m.reply_text("📥 Reading your file...")
>
>         try:
>             file_path = await client.download_media(m, in_memory=True)
>             text = bytes(file_path.getbuffer()).decode("utf-8", errors="ignore")
>         except Exception as e:
>             await status.edit_text(f"❌ Failed to read file: {e}")
>             return
>
>         topics = parse_topics_from_text(text)
>
>         if not topics:
>             await status.edit_text("❌ No topics found in this file.\nMake sure lines look like:\n`📌 Topic Name — 123 links`")
>             return
>
>         # Save state for this user
>         _user_state[m.from_user.id] = {"state": "WAIT_GROUP_ID", "topics": topics}
>
>         topic_list = "\n".join(f"{i+1}. {t}" for i, t in enumerate(topics))
>         await status.edit_text(
>             f"✅ Found **{len(topics)} topics**:\n\n{topic_list}\n\n"
>             f"Now send me the **Group Chat ID** where I should create these topics.\n"
>             f"_(It looks like: `-1001234567890`)_"
>         )
>
>     @bot.on_message(filters.private & filters.text & ~filters.command(["start","stop","id","info","logs","reset","cookies","getcookies","ytcookies","ytcookie","y2t","ytm","t2t","t2h","json","history","yth","viewhistory","clearhistory","createtopic","topics","settopic","setuptopics","parsetxt","defaulttopic","parsetopics","topicid","gettopicid","linktopics","showtopics","showmapping","clearmemory","mini","topicnav","broadcast","broadusers","addauth","rmauth","users","allhistory","resetallhistory","addaccount","listaccounts","removeaccount"]))
>     async def handle_group_id_input(client: Client, m: Message):
>         user_id = m.from_user.id
>         state_data = _user_state.get(user_id)
>
>         # Only handle if this user is in WAIT_GROUP_ID state
>         if not state_data or state_data.get("state") != "WAIT_GROUP_ID":
>             return
>
>         # Validate group chat ID
>         raw_id = m.text.strip()
>         try:
>             group_chat_id = int(raw_id)
>         except ValueError:
>             await m.reply_text("❌ Invalid Chat ID. Please send a number like `-1001234567890`")
>             return
>
>         topics = state_data["topics"]
>         total = len(topics)
>         created = 0
>         failed = 0
>
>         # Clear state immediately so duplicate messages don't retrigger
>         _user_state.pop(user_id, None)
>
>         progress_msg = await m.reply_text(f"⏳ Starting topic creation for **{total} topics**...")
>
>         for i, topic_name in enumerate(topics, start=1):
>             try:
>                 await client.invoke(
>                     __import__("pyrogram.raw.functions.channels", fromlist=["CreateForumTopic"]).CreateForumTopic(
>                         channel=await client.resolve_peer(group_chat_id),
>                         title=topic_name,
>                         random_id=__import__("random").randint(1, 2**31)
>                     )
>                 )
>                 created += 1
>                 await progress_msg.edit_text(f"⏳ ({i}/{total}) ✅ Created: **{topic_name}**")
>             except Exception as e:
>                 err_str = str(e)
>                 failed += 1
>                 logging.warning(f"[AutoTopic] Failed to create '{topic_name}': {e}")
>
>                 if "CHAT_ADMIN_REQUIRED" in err_str or "not enough rights" in err_str.lower():
>                     await progress_msg.edit_text(
>                         f"❌ Bot is not admin or doesn't have **Manage Topics** permission in the group.\n"
>                         f"Please make bot admin and grant that permission, then try again."
>                     )
>                     return
>
>                 if "chat not found" in err_str.lower() or "CHANNEL_INVALID" in err_str:
>                     await progress_msg.edit_text(
>                         f"❌ Group not found. Check the Chat ID and make sure bot is a member of the group."
>                     )
>                     return
>
>                 await progress_msg.edit_text(f"⏳ ({i}/{total}) ❌ Failed: **{topic_name}**\n`{err_str[:100]}`")
>
>             await asyncio.sleep(1.5)
>
>         await progress_msg.edit_text(
>             f"🏁 **Done!**\n\n✅ {created} created\n❌ {failed} failed\n\n"
>             f"Send another `.txt` file to start again."
>         )
> ```
>
> ---
>
> ### STEP 2 — Add exactly 2 lines to `main.py`
>
> **Line 1:** In the imports block (after line 21 where `mini_handler` is imported), add:
> ```python
> from auto_topic_creator import register_auto_topic_handlers
> ```
>
> **Line 2:** After line 198 where `register_render_manager_handlers(bot)` is called, add:
> ```python
> register_auto_topic_handlers(bot)
> ```
>
> **Do not change anything else in `main.py`. Not a single other line.**
>
> ---
>
> ### HOW IT WORKS (no new commands needed)
> - User sends a `.txt` file in private chat → bot auto-detects it, parses topics, asks for group ID
> - User replies with the group Chat ID → bot creates all topics one by one with progress updates
> - No command needed. No new bot command to register.
>
> ---
>
> ### IMPORTANT NOTES
> - This uses **Pyrogram** (same as the whole existing project) — do NOT use `python-telegram-bot` or `aiogram`
> - Do NOT install any new packages — Pyrogram is already installed
> - The `_user_state` dict is in-memory per session — this is intentional and correct
> - The `.txt` filter runs only if another handler hasn't already consumed the document message — this is safe because existing handlers target specific commands, not raw documents

---

After Replit builds it, make sure:
- Bot is **admin** in the group with **Manage Topics** permission
- Group has **Topics/Forums enabled** in settings
- Use `/id` command (already in your bot) to get the group Chat ID